<!DOCTYPE html>
<html>
  <head>
  </head>
  <body>
    <% if Helpers.is_logged_in?(session) %>
      <p>Welcome <%= Helpers.current_user(session).username %></p>
      <p>Your Balance: <%= Helpers.current_user(session).balance %></p>
    
      <p><a href="/logout">Log Out</a></p>
    <% end %>

  </body>
</html>